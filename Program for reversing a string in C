#include <stdio.h>
#include <string.h>
int main()
{
    int i;
    char temp;
    char str[30]="string";
    int l=strlen(str);
    
    for(i=0;i<l/2;i++)
    {
        temp=str[i];
        str[i]=str[l-1-i];
        str[l-i-1]=temp;
    }
    printf("%s",str);
    return 0;
}
